app = 'leihapp'
primary_region = 'ams'

[build]
  build-target = 'prod'

[env]
  NUXT_PUBLIC_POCKETBASE_SERVER_BASE_URL = 'https://leihapp-pocketbase.fly.dev'
  NUXT_PUBLIC_POCKETBASE_CLIENT_BASE_URL = 'https://leihapp-pocketbase.fly.dev'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
